@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">
    @if (!string.IsNullOrEmpty(Model.Error)) {
        <strong>@Model.Error</strong>
    }
    else
    {
        @:Error.
    }
</h1>
<h2 class="text-danger">
    @if (!string.IsNullOrEmpty(Model.ErrorDescription)) {
        <small>@Model.ErrorDescription</small>
    }
    else
    {
        @:Se produjo un error al procesar su solicitud.
    }
</h2>

@if(Model != null && Model.ShowRequestId)
{
    <p>
        <strong>Solicitud ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de desarrollo</h3>
<p>
    Cambiando a <strong>desarrollo</strong> el entorno mostrará información más detallada sobre el error que se produjo..
</p>
<p>
    <strong>El entorno de desarrollo no debe estar habilitado en la aplicación desplegadas</strong>, ya que puede dar lugar a información confidencial de las excepciones que se muestran a los usuarios finales. Para la depuración local, el entorno de desarrollo se puede habilitar configurando <strong>ASPNETCORE_ENVIRONMENT</strong> variable de entorno a <strong>Desarrollo</strong>, y reiniciando la aplicación.
</p>

@{
    Layout = "../Shared/_NavLayout.cshtml";
    ViewData.SetActivePageAndTitle(StoreNavPages.PayButton, "Confirma que deseas permitir que el mundo exterior cree facturas en tu tienda (a través de API).");
    ViewBag.MainTitle = "Botón de pago";
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-10">
        <form method="post">
            <div class="form-group">
                <p>
                    Para comenzar a utilizar Botones de pago, debe activar explícitamente esta función. Una vez que lo haga, cualquier entidad de terceros podrá crear una factura en su almacén de instancia (por ejemplo, a través de API).
                </p>
                @Html.Hidden("EnableStore", true)
                <button name="command" type="submit" value="save" class="btn btn-primary">Permitir que el mundo exterior cree facturas a través de API (la aplicación POS está autorizada previamente y no necesita que esté habilitada).</button>
            </div>
        </form>
    </div>
</div>

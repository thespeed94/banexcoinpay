@model SignWithSeedViewModel
@{
    Layout = "../Shared/_NavLayout.cshtml";
    ViewData["Title"] = "Firma PSBT con una clave privada HD o seed mnemotécnica";
    ViewData.SetActivePageAndTitle(WalletsNavPages.Send);
}

<h4>Firma PSBT con una clave privada HD o seed mnemotécnica</h4>
<div class="row">
    <div class="col-md-10">
        <div class="alert alert-warning alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <p>
                <span>Antes de continuar, por favor, comprenda lo siguiente:</span>
            </p>
            <ol>
                <li>Asegúrese de que esta instancia de BTCPay Server <b>te pertenece</b>.</li>
                <li>Utilizar <b>el modo incógnito de Chrome o el navegador Tor</b>, esto asegura que no se esté ejecutando ningún complemento de navegador malicioso que pueda robar su clave.</li>
            </ol>
            <p>
                <span>De lo contrario, se está exponiendo a un propietario de sitio malicioso o a un complemento malicioso instalado en su navegador..</span>
            </p>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-md-10">
        <div asp-validation-summary="All" class="text-danger"></div>
        <form method="post" asp-action="SignWithSeed">
            <input type="hidden" asp-for="PSBT" />
            <div class="form-group">
                <label asp-for="SeedOrKey"></label>
                <input asp-for="SeedOrKey" class="form-control" />
                <span asp-validation-for="SeedOrKey" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Passphrase"></label>
                <input asp-for="Passphrase" class="form-control" />
                <span asp-validation-for="Passphrase" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Ingresar</button>
        </form>
    </div>
</div>
